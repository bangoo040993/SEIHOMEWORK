config:
  target: 'http://localhost:3000'
  phases:
    - duration: 30
      arrivalRate: 30
scenarios:
  - flow:
      - get:
          url: "/todos"
          # json:
            # name: "Test"
            # email: "test@example.com"
            # password: "pazziword69420"